spring:
  datasource:
    url: jdbc:postgresql://localhost:6969/politicalArguments
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate:
      ddl-auto: create

jwt:
  secret: mySecretToken
  expirationTime: 86400000

